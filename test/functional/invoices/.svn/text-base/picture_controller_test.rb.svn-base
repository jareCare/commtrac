require File.dirname(__FILE__) + '/../../test_helper'
require 'invoices/picture_controller'

# Re-raise errors caught by the controller.
class Invoices::PictureController; def rescue_action(e) raise e end; end

class Invoices::PictureControllerTest < Test::Unit::TestCase
  def setup
    @controller = Invoices::PictureController.new
    @request    = ActionController::TestRequest.new
    @response   = ActionController::TestResponse.new
  end

  # Replace this with your real tests.
  def test_truth
    assert true
  end
end
